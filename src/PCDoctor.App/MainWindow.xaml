<Window x:Class="PCDoctor.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource App.Name}"
        Icon="pack://application:,,,/Assets/app.ico"
        Width="1380"
        Height="880"
        MinWidth="1120"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="248"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource SidebarBackgroundBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="18,22,18,12">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/logo.png"
                               Width="36"
                               Height="36"
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource Shell.Brand}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="{DynamicResource Shell.Subtitle}"
                                       Style="{StaticResource MutedText}"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                <TextBlock DockPanel.Dock="Bottom"
                           Margin="22,0,22,18"
                           Text="{DynamicResource Shell.ReadOnly}"
                           Style="{StaticResource MutedText}"
                           FontSize="11"/>
                <ListBox Style="{StaticResource NavListBox}"
                         ItemsSource="{Binding Navigation}"
                         SelectedItem="{Binding SelectedNavigation}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}"
                                           FontFamily="{StaticResource IconFont}"
                                           FontSize="16"
                                           Width="22"
                                           Foreground="{DynamicResource AccentBrush}"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentControl Margin="28,24,20,8" Content="{Binding CurrentView}"/>
            <Border Grid.Row="1"
                    Background="{DynamicResource SidebarBackgroundBrush}"
                    BorderBrush="{DynamicResource DividerBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="28,10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Text="Tondy Pc Doctor 1.2" Style="{StaticResource MutedText}"/>
                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource MutedText}"/>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
