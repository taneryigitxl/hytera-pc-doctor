<Application x:Class="PCDoctor.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:PCDoctor.App.Converters"
             xmlns:views="clr-namespace:PCDoctor.App.Views"
             xmlns:vm="clr-namespace:PCDoctor.App.ViewModels">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Colors.Dark.xaml"/>
                <ResourceDictionary Source="Themes/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
            <converters:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibility"/>
            <converters:SeverityToBrushConverter x:Key="SeverityToBrush"/>
            <converters:InverseBooleanConverter x:Key="InverseBoolean"/>
            <converters:BooleanToStatusConverter x:Key="BooleanToStatus"/>
            <converters:SeverityToTextConverter x:Key="SeverityToText"/>

            <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                <views:DashboardView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ScanViewModel}">
                <views:ScanView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:HardwareViewModel}">
                <views:HardwareView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:NetworkViewModel}">
                <views:NetworkView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:WindowsViewModel}">
                <views:WindowsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:EventLogsViewModel}">
                <views:EventLogsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:StartupViewModel}">
                <views:StartupView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SecurityViewModel}">
                <views:SecurityView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:OverlaySettingsViewModel}">
                <views:OverlaySettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TroubleshooterViewModel}">
                <views:TroubleshooterView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReportsViewModel}">
                <views:ReportsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
