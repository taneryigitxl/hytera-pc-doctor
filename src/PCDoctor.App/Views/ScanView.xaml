<UserControl x:Class="PCDoctor.App.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:PCDoctor.App.Views.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8,0,16,16">
            <TextBlock Text="{DynamicResource Scan.Title}" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="{DynamicResource Scan.Subtitle}" Style="{StaticResource PageSubtitle}"/>

            <Border Style="{StaticResource CardBorder}" Margin="0,18,0,12">
                <StackPanel>
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Content="{DynamicResource Scan.Start}"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding StartCommand}"
                                    Margin="0,0,8,0"/>
                            <Button Content="{DynamicResource Scan.Fix}"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding FixCommand}"
                                    Margin="0,0,8,0"/>
                            <Button Content="{DynamicResource Scan.Cancel}"
                                    Style="{StaticResource GhostButton}"
                                    Command="{Binding CancelCommand}"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="{Binding CurrentModuleLabel}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Status}" Style="{StaticResource MutedText}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </DockPanel>
                    <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Height="10" Margin="0,16,0,8"/>
                    <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0}%}" Style="{StaticResource MutedText}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ErrorBanner}"
                    Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibility}}">
                <TextBlock Text="{Binding ErrorMessage}"/>
            </Border>

            <UniformGrid Columns="3" Visibility="{Binding HasReport, Converter={StaticResource BooleanToVisibility}}">
                <Border Style="{StaticResource CardBorder}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Scan.Ok}" Style="{StaticResource SectionLabel}"/>
                        <TextBlock Text="{Binding OkCount}" Style="{StaticResource MetricValue}" Foreground="{DynamicResource SeverityOkBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Scan.Warning}" Style="{StaticResource SectionLabel}"/>
                        <TextBlock Text="{Binding WarningCount}" Style="{StaticResource MetricValue}" Foreground="{DynamicResource SeverityWarningBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Scan.Critical}" Style="{StaticResource SectionLabel}"/>
                        <TextBlock Text="{Binding CriticalCount}" Style="{StaticResource MetricValue}" Foreground="{DynamicResource SeverityCriticalBrush}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <ItemsControl ItemsSource="{Binding Findings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:FindingView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
